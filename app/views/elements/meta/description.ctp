<?php
if (!isset($desc)) {
  $desc = __('Greenpeace exists because this fragile Earth deserves a voice. It needs solutions. It needs change. It needs action. ',true);
}
?>
  <meta name="description" content="<?php echo $desc; ?>" />
